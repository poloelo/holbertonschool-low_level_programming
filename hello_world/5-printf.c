#include <stdio.h>
/**
 * main - use printf function to print hatever is in the function
 * Return: returns 0 if everything went well
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
